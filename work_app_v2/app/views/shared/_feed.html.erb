<div class="container">
	<div class="row">
		<div class="col-sm-8">
			<% if @feed_items.any? %>
				<ol class="microposts">
				    <%= render @feed_items %>
				</ol>
				    <%= will_paginate @feed_items %>
			<% end %>
		</div>
	</div>
</div>
